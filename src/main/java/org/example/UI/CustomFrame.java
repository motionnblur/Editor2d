package org.example.UI;

import javax.swing.*;
import java.awt.*;

public class CustomFrame extends JFrame {
    JFrame frame;
    public CustomFrame() {
        frame = new JFrame();
        setBackground(Color.BLACK);
    }
}
